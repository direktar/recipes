%section.w-full
  %h1.mb-8.text-3xl.font-bold.text-white Edit Pantry

  /––– Add‑new form ––––––––––––––––––––––––––––––––––––––––––
  = form_with @user_ingredient,
              url: user_ingredients_path,
              class: "mb-10 flex flex-wrap gap-4" do |f|

    = f.collection_select :ingredient_id,
                          Ingredient.order(:name), :id, :name,
                          { prompt: "Select ingredient" },
                          class: "w-60 rounded border-gray-300 bg-gray-100 text-black"

    = f.number_field :quantity, step: 0.1,
          placeholder: "Qty",
          class: "w-24 rounded border-gray-300 bg-gray-100 text-black"

    = f.text_field :unit,
          placeholder: "Unit",
          class: "w-24 rounded border-gray-300 bg-gray-100 text-black"

    = f.submit "Add",
          class: "rounded bg-indigo-600 px-4 py-1 font-medium text-white hover:bg-indigo-500"

  /––– List & actions ––––––––––––––––––––––––––––––––––––––––
  %table.w-full.text-left.text-gray-200
    %thead.bg-gray-800
      %tr
        %th.px-4.py-2 Ingredient
        %th.px-4.py-2 Qty
        %th.px-4.py-2 Unit
        %th.px-4.py-2
    %tbody
      - @user_ingredients.each do |ui|
        %tr.border-b.border-gray-700
          %td.px-4.py-2= ui.ingredient.name.titleize
          %td.px-4.py-2= ui.quantity
          %td.px-4.py-2= ui.unit
          %td.px-4.py-2.space-x-3
            = link_to "✎",
                      edit_user_ingredient_path(ui),
                      class: "text-yellow-400 hover:text-yellow-300"

            = link_to "🗑",
                      user_ingredient_path(ui),
                      method: :delete,
                      data: { turbo_confirm: "Remove?" },
                      class: "text-red-500 hover:text-red-400"
